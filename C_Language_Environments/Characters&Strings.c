#include <stdio.h>

int main() {

    char one = 70;
    char two = 'q';
    
    printf("\nOne as a character:\t%c", one);
    printf("\nOne as a number:\t%d", one);
    printf("\nTwo as a character:\t%c", two);
    printf("\nTwo as a number:\t%d", two);

    return(0);
}
